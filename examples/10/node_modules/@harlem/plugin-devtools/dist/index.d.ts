import { HarlemPlugin } from '@harlem/core';

interface Options {
    label: string;
    color: number;
}

declare function createDevtoolsPlugin(options?: Partial<Options>): HarlemPlugin;

export { createDevtoolsPlugin as default };
